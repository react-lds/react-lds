import React from 'react';
import { Pill } from 'react-lds';

const Example = () => (
  <Pill label="Pill Label" title="Full pill label verbiage mirrored here" onClose={() => {}} />
);

export default Example;
